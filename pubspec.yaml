name: cozkazan
description: A new Flutter project.

publish_to: 'none' # Remove this line if you want to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  open_file: ^3.3.2
  screen_brightness: ^2.1.5
  wakelock_plus: ^1.1.4
  dio: ^5.4.0
  path_provider: ^2.1.2
  shared_preferences: ^2.2.2
  http: ^1.4.0
  confetti: ^0.8.0
  fl_chart: ^1.0.0
  url_launcher: ^6.2.5
  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.6.12
  firebase_storage: ^12.4.10
  speech_to_text: ^6.6.0
  # cloud_firestore: ^4.15.5
  # firebase_core: ^2.25.4
  # diğer mevcut paketlerin burada kalmasına dikkat et

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/