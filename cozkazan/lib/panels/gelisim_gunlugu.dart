import 'package:flutter/material.dart';

class GelisimGunlugu extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Gelişim Günlüğü')),
      body: Center(child: Text('Gelişim günlüğü içeriği burada olacak')),
    );
  }
} 